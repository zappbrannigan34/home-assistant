# Adaptive Humidity Control Automation

Автоматическое управление увлажнителем Polaris PUH‑9105 на основе комнатной и уличной температуры.

---

## 🤔 Зачем это нужно?

Зимой при проветривании и работе отопления влажность в комнате резко падает:

- холодный воздух с улицы содержит очень мало воды в абсолютном выражении;
- при нагреве до комнатной температуры его относительная влажность падает в разы (10–20% и ниже);
- результат – сухость, раздражение слизистых, статическое электричество.

Эта автоматизация решает задачу **поддержания комфортной и безопасной влажности**:

- с опорой на рекомендации **ВОЗ (40–60% RH)**;
- с учётом температуры **в комнате** и **на улице**;
- с автоматическим управлением питанием и мощностью увлажнителя.

---

## 🌍 Научная основа и целевая влажность

### Источники

- **ВОЗ / WHO** – рекомендуемый диапазон 40–60% относительной влажности для здоровья и снижения распространения вирусов/бактерий.
- **ASHRAE / REHVA / Aprilaire** – графики зависимости допустимой влажности от наружной температуры.
- **ISO 7730** – общее понимание теплового комфорта при различных температурах.

### Целевая влажность (Adaptive Target Humidity)

Цель рассчитывается в три шага:

1. **От комнатной температуры (`T_room`)**:

   ```text
   base_indoor = 75 − 1.25 × T_room
   ```

   Примеры:

   | T_room | base_indoor |
   |--------|-------------|
   | 18°C   | 52.5%       |
   | 20°C   | 50.0%       |
   | 22°C   | 47.5%       |
   | 24°C   | 45.0%       |
   | 26°C   | 42.5%       |

2. **От уличной температуры (`T_outdoor`)**:

   ```text
   outdoor_limit = 50 − max(0, 10 − T_outdoor)
   ```

   Примеры:

   | Улица       | outdoor_limit |
   |-------------|---------------|
   | > +10°C     | 50%           |
   | +5°C        | 45%           |
   | 0°C         | 40%           |
   | −10°C       | 30%           |
   | −20°C       | 20%           |
   | −30°C       | 10%           |

3. **Итоговая цель с ограничениями ВОЗ**:

   ```text
   target_raw = min(base_indoor, outdoor_limit, 60)
   target     = clamp(target_raw, 40, 60)
   ```

   То есть итоговая цель **всегда в диапазоне 40–60%**:

   - минимальное значение 40% – медицинский минимум по ВОЗ;
   - максимальное значение 60% – верхняя комфортная граница;
   - ГОСТ 30–45% в этой автоматизации **не используется** – приоритет у диапазона ВОЗ.

---

## 🧮 Ошибка и гибридный алгоритм мощности

### Ошибка регулирования

**Sensor: `sensor.humidity_error`**

```text
error = target − current
```

- `error > 0` → воздух суше цели (недоувлажнение);
- `error < 0` → воздух влажнее цели (переувлажнение).

### Гибридный алгоритм (HYBRID)

**Sensor: `sensor.recommended_intensity`**

Используется сочетание:

- **Zoned control** – для больших отклонений;
- **Incremental control** – для малых отклонений;
- **Гистерезис ±1%** – для подавления дребезга.

#### Zoned Control (большие отклонения)

```text
error > 10%           → intensity = 7  (очень сухо → максимум)
5% < error ≤ 10%      → intensity = 5
2% < error ≤ 5%       → intensity = 3
```

#### Incremental Control (вблизи цели)

```text
1% < error ≤ 2%       → intensity = current_intensity + 1  (до max 7)
error < −1%           → intensity = current_intensity − 1  (до min 0)
−1% ≤ error ≤ 1%      → intensity не менять (гистерезис)
```

Дополнительно:

- интенсивность **не меняется чаще, чем раз в 5 минут**, даже если error часто перескакивает пороги;
- это защищает увлажнитель от частых переключений и увеличивает ресурс.

---

## ⚙️ Логика AUTO / MANUAL

### AUTO

Режим AUTO считается включённым, когда активна автоматизация:

- `automation.humidity_control_main`

При этом работают:

- `automation.humidity_device_setup` – инициализация устройства при входе в AUTO;
- `automation.humidity_manual_override_detector` – детектор ручного управления;
- `automation.humidity_device_error` – уведомления об ошибках.

### MANUAL

MANUAL включается автоматически при **любом внешнем управлении увлажнителем**:

- изменении:
  - `number.humidifier_puh_9105_puh_2709_intensity`
  - `switch.humidifier_puh_9105_puh_2709_power`
  - `humidifier.humidifier_puh_9105_puh_2709_humidifier` (атрибут `mode`)
  - `switch.humidifier_puh_9105_puh_2709_warm_stream`
  - `switch.humidifier_puh_9105_puh_2709_backlight`
  - `switch.humidifier_puh_9105_puh_2709_ioniser`
  - `switch.humidifier_puh_9105_puh_2709_sound`
- и это изменение **не инициировано** самими автоматизациями.

**Automation: `humidity_manual_override_detector`**:

- отслеживает все эти изменения;
- отфильтровывает:
  - инициализацию entity (`unavailable/unknown` → нормальное значение),
  - изменения во время выполнения `humidity_control_main` и `humidity_device_setup`;
- при обнаружении ручного управления **выключает только**:
  - `automation.humidity_control_main`.

В MANUAL автоматическое управление интенсивностью и питанием приостанавливается.

### Возврат в AUTO

Для возврата в AUTO используется скрипт:

- **`script.humidity_auto_resume`**

Он:

1. Включает:
   - `automation.humidity_control_main`
   - `automation.humidity_manual_override_detector`
2. При включении `humidity_control_main` срабатывает:
   - `automation.humidity_device_setup`, которая:
     - включает питание,
     - ставит режим `home`,
     - настраивает звук/подсветку/подогрев/ионизацию/блокировку,
     - ждёт стабилизации MQTT.

Скрипт можно:

- запускать из UI HA (Developer Tools → Services → `script.turn_on`);
- экспортировать в `yandex_smart_home` как устройство (тип `switch`/`scene`) и запускать голосом.

---

## 🔧 Состав пакета

### Template Sensors

- `sensor.adaptive_target_humidity` – адаптивная целевая влажность (ВОЗ 40–60%, зависит от T_room и T_outdoor).
- `sensor.humidity_error` – ошибка регулирования (target − current).
- `sensor.recommended_intensity` – рекомендуемая интенсивность (0–7) по HYBRID‑алгоритму.

### Scripts

- `script.humidity_auto_resume` – запуск AUTO‑режима (включает основную автоматику и детектор).

### Automations

- `automation.humidity_device_setup` – инициализация увлажнителя при входе в AUTO.
- `automation.humidity_control_main` – основное управление интенсивностью и питанием (с ограничением «не чаще 1 раза в 5 минут»).
- `automation.humidity_manual_override_detector` – переход в MANUAL при ручном управлении (пульт, кнопки, интеграции).
- `automation.humidity_device_error` – уведомления при ошибках устройства.

Отдельный `input_boolean` для включения AUTO **не используется** – роль переключателя играет состояние `automation.humidity_control_main` и скрипт `humidity_auto_resume`.

---

## 🚀 Установка и первый запуск

1. Скопируйте YAML‑файл пакета в `config/packages/humidity/humidity_control.yaml` (или другое имя по вашей структуре).
2. Убедитесь, что в `configuration.yaml` подключены пакеты:

   ```yaml
   homeassistant:
     packages: !include_dir_named packages
   ```

3. Замените entity_id в пакете на свои (см. раздел про настройку Entity IDs).
4. Перезагрузите Home Assistant или выполните Reload Template Entities / Scripts / Automations.
5. **Один раз** выполните `script.humidity_auto_resume`, чтобы войти в AUTO.

После этого:

- увлажнитель будет автоматически поддерживать целевую влажность;
- любое внешнее управление переведёт его в MANUAL;
- возвращение в AUTO – тем же скриптом.

---

**Device:** Polaris PUH‑9105 / PUH‑2709  
**Integration:** `polaris-mqtt` (MQTT)  
**Version пакета:** 2.0.1