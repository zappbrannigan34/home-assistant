# Adaptive Humidity Control Automation

Автоматическое управление увлажнителем Polaris PUH‑9105 на основе PD-регулятора с предиктивным демпфированием.

---

## 🤔 Зачем это нужно?

Зимой при проветривании и работе отопления влажность в комнате резко падает:

- холодный воздух с улицы содержит очень мало воды в абсолютном выражении;
- при нагреве до комнатной температуры его относительная влажность падает в разы (10–20% и ниже);
- результат – сухость, раздражение слизистых, статическое электричество.

Эта автоматизация решает задачу **поддержания комфортной и безопасной влажности**:

- с опорой на рекомендации **ВОЗ (40–60% RH)**;
- с учётом температуры **в комнате** и **на улице**;
- с автоматическим управлением питанием и мощностью увлажнителя;
- с виртуальным датчиком уровня воды и автокалибровкой расхода per-level.

---

## 🌍 Научная основа и целевая влажность

### Источники

- **ВОЗ / WHO** – рекомендуемый диапазон 40–60% относительной влажности.
- **ASHRAE / REHVA / Aprilaire** – графики зависимости допустимой влажности от наружной температуры.

### Целевая влажность (Adaptive Target Humidity)

Цель рассчитывается в три шага:

1. **От комнатной температуры (`T_room`)**:

   ```text
   base_indoor = 75 − 1.25 × T_room
   ```

   | T_room | base_indoor |
   |--------|-------------|
   | 20°C   | 50.0%       |
   | 22°C   | 47.5%       |
   | 24°C   | 45.0%       |

2. **От уличной температуры (`T_outdoor`)**:

   ```text
   outdoor_limit = 50 − max(0, 10 − T_outdoor)
   ```

   | Улица   | outdoor_limit |
   |---------|---------------|
   | +5°C    | 45%           |
   | 0°C     | 40%           |
   | −10°C   | 30%           |
   | −20°C   | 20%           |

3. **Итоговая цель**:

   ```text
   target = clamp(min(base_indoor, outdoor_limit, 60), 40, 60)
   ```

---

## 🧮 PD-регулятор с предиктивным демпфированием

### Ошибка регулирования

**Sensor: `sensor.humidity_error`**

```text
error = target − current
```

### Скорость изменения влажности

**Sensor: `sensor.humidity_rate`** (%/мин)

- Кольцевой буфер 5 шагов × 1 мин = 5-минутное окно
- EMA-сглаживание (0.4 new + 0.6 old)

### Рекомендуемая интенсивность

**Sensor: `sensor.recommended_intensity`**

1. **Прогноз ошибки** на 7 минут вперёд:
   ```text
   predicted_error = error − (rate × 7)
   ```

2. **Определение интенсивности по прогнозу**:
   - `predicted_error > DEADBAND (2.0)` → **7** (максимум, агрессивное увлажнение)
   - `−DEADBAND..+DEADBAND` → поддержание текущей или плавное снижение
   - `predicted_error < −DEADBAND` → снижение до 1 или 0

3. **Адаптивное демпфирование** — при быстром приближении к цели интенсивность снижается превентивно

4. **Rate limit**: +3 вверх, −2 вниз за один шаг

5. **Антидребезг**: интенсивность не меняется чаще 1 раза в 30 секунд

---

## 💧 Виртуальный датчик уровня воды

### Принцип

Увлажнитель не имеет датчика уровня воды — только бинарный сигнал `low_water`.
Пакет реализует **виртуальный датчик** с автокалибровкой расхода на каждом уровне интенсивности.

### Как это работает

1. **Каждую минуту** считается расход воды по калиброванной ставке текущего уровня:
   ```text
   consumed += rate_ml_h[intensity] / 60
   ```

2. **Уровень воды** = `(capacity − consumed) / capacity × 100%`

3. **Трекинг минут**: для каждого уровня (1–7) считается сколько минут он работал в текущем цикле (от заправки до опустошения).

### Автокалибровка per-level

При опустошении бака (`low_water`):

1. Известно: ёмкость бака = 5000 мл (константа)
2. Известно: модель насчитала `model_consumed` мл
3. Коэффициент коррекции: `correction = 5000 / model_consumed`
4. Для каждого уровня с >5 минутами работы — корректируем ставку (EMA-сглаживание по циклам)
5. Уровни с <5 минутами — не трогаем (недостаточно данных)

**Результат**: уровни которые используются чаще — калибруются точнее. После нескольких циклов модель расхода становится точной.

### Хранение данных

| Entity | Назначение |
|--------|-----------|
| `input_number.humidifier_tank_capacity_ml` | Ёмкость бака, мл (константа, по умолчанию 5000) |
| `input_number.humidifier_calibration_cycles` | Количество циклов калибровки |
| `input_text.humidifier_consumption_rates` | JSON: калиброванные ставки мл/ч для уровней 1–7 |
| `input_text.humidifier_level_minutes` | JSON: минуты работы на каждом уровне в текущем цикле |

Все значения персистятся в `.storage` и сохраняются при перезагрузке HA.

### Начальные ставки (линейная модель)

| Уровень | мл/ч  |
|---------|-------|
| 1       | 71.4  |
| 2       | 142.9 |
| 3       | 214.3 |
| 4       | 285.7 |
| 5       | 357.1 |
| 6       | 428.6 |
| 7       | 500.0 |

---

## 🔧 Состав пакета

### Template Sensors

| Sensor | Описание |
|--------|----------|
| `sensor.adaptive_target_humidity` | Адаптивная целевая влажность (40–60%) |
| `sensor.humidity_error` | Ошибка регулирования (target − current) |
| `sensor.humidity_rate` | Скорость изменения влажности (%/мин) |
| `sensor.recommended_intensity` | PD-регулятор: рекомендуемая интенсивность (0–7) |
| `sensor.humidifier_water_consumed` | Накопленный расход воды (мл) |
| `sensor.humidifier_water_level` | Уровень воды в баке (%) |

### Automations

| ID | Назначение |
|----|-----------|
| `humidity_device_setup` | Инициализация устройства при старте/включении |
| `humidity_mode_enforcer` | Гарантирует режим `home` |
| `humidity_control_main` | Основная логика управления интенсивностью и питанием |
| `humidifier_track_level_minutes` | Трекинг минут работы на каждом уровне |
| `humidifier_calibrate_rates` | Автокалибровка расхода per-level при опустошении |
| `humidity_device_error` | Уведомления об ошибках устройства |

### Scripts

| Script | Назначение |
|--------|-----------|
| `script.humidity_auto_resume` | Возобновление автоматического управления |

---

## 🚀 Установка и первый запуск

1. Скопируйте YAML‑файл пакета в `config/packages/humidity/humidity_control.yaml`.
2. Убедитесь, что в `configuration.yaml` подключены пакеты:

   ```yaml
   homeassistant:
     packages: !include_dir_named packages
   ```

3. Замените entity_id в пакете на свои.
4. **Перезагрузите Home Assistant** (полный рестарт — reload YAML не создаст `input_number`, `input_text` и trigger-based sensors).

---

**Device:** Polaris PUH‑9105 / PUH‑2709  
**Integration:** `polaris-mqtt` (MQTT)  
**Version пакета:** 3.1.0
